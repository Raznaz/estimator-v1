# Подключение к PostgreSQL (см. корневой .env / docker-compose.yml)
DATABASE_URL=postgresql://estimator:estimator@localhost:5432/estimator?schema=public

# Порт NestJS
API_PORT=3001

# Разрешённый источник для CORS (фронтенд)
CORS_ORIGIN=http://localhost:3000
